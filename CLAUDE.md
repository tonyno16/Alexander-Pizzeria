# CLAUDE.md

## Meta Ads

Per qualsiasi attività su Meta Ads (creazione campagne, ad set, upload creatività, analisi performance), usa gli script Python in `scripts/meta_ads_*.py` basati sull'SDK ufficiale `facebook-business`. Token e ad account ID sono in `.env`.
